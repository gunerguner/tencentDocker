# 复制为 `.env` 后按需修改（不要将含机密的文件提交到 Git）。
# 在 `docker/` 目录下执行：`docker compose --env-file .env up -d`

# 上游为各项目 compose 映射到宿主机的「前端」端口（见 carSales / stockManager 的 FRONTEND_PUBLISH_PORT）。
# 默认与两项目 README 约定一致：stockManager 8080，carSales 8081。
# 使用 host.docker.internal 需配合 compose 中的 extra_hosts: host-gateway（已写好）。

STOCK_FRONTEND_UPSTREAM=host.docker.internal:8080
CARSALES_FRONTEND_UPSTREAM=host.docker.internal:8081
